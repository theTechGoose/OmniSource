import { Controller, NoAuth } from "@shared/denomite";

@Controller
export class BooksiActText extends NoAuth {
  "POST /opt-contact-in" = '';
  "GET /opt-contact-in"  = '';
  "POST /bland-transfer" = '';
}
