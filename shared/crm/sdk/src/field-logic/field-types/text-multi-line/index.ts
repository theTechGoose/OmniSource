import { validator } from '../text/validator';
import { coorcer } from '../text/coorcer';
import { formatter } from '../text/formatter';

export const textMultiLine = {
  validator,
  coorcer,
  formatter
}
