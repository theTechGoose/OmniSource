import { validator } from './validator';
import { coorcer } from '../text/coorcer';
import { formatter } from '../text/formatter';

export const textMultipleChoice = {
  validator,
  coorcer,
  formatter
}
